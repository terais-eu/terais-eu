# The Native Font Theme provides optimum text rendering on every device and OS.
# Note that the native font families are automatically populated by Academic.

# Font style metadata
name = "TERAIS"

# Optional Google font URL
google_fonts = "family=Red+Hat+Mono:ital,wght@0,400;0,700;1,400;1,700"

# Font families
heading_font = "Helvetica"
body_font = "Helvetica"
nav_font = "Helvetica"
mono_font = "Red Hat Mono"
